from crud import *
if __name__ == '__main__':
    while True:
        op = menu()
        if op == "c":
            nomes = create()
        elif op == "r":
            ler_lista = read()
        elif op == "u":
            atualizar_lista = update()
        elif op == "d":
            deleta = delete()
        else:
            break
